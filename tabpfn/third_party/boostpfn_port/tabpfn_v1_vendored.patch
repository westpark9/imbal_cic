diff -ruN -x __pycache__ tabpfn_v1/encoders.py tabpfn_v1/encoders.py
--- tabpfn_v1/encoders.py	2026-09-08 13:41:29.936209740 +0900
+++ tabpfn_v1/encoders.py	2026-09-04 15:30:53.555779416 +0900
@@ -2,7 +2,7 @@
 
 import torch
 import torch.nn as nn
-from tabpfn.utils import normalize_data
+from tabpfn_v1.utils import normalize_data
 import torch.nn.functional as F
 from torch.nn import TransformerEncoder, TransformerEncoderLayer
 
diff -ruN -x __pycache__ tabpfn_v1/__init__.py tabpfn_v1/__init__.py
--- tabpfn_v1/__init__.py	2026-09-08 13:41:29.936188612 +0900
+++ tabpfn_v1/__init__.py	2026-09-04 15:30:53.555207836 +0900
@@ -1 +1 @@
-from tabpfn.scripts.transformer_prediction_interface import TabPFNClassifier
+from tabpfn_v1.scripts.transformer_prediction_interface import TabPFNClassifier
diff -ruN -x __pycache__ tabpfn_v1/layer.py tabpfn_v1/layer.py
--- tabpfn_v1/layer.py	2026-09-08 13:41:29.936234854 +0900
+++ tabpfn_v1/layer.py	2026-09-04 15:35:21.728061115 +0900
@@ -2,7 +2,11 @@
 
 from torch import nn
 import torch
-from torch.nn.modules.transformer import _get_activation_fn, Module, Tensor, Optional, MultiheadAttention, Linear, Dropout, LayerNorm
+# torch>=2.x no longer re-exports typing/nn names from torch.nn.modules.transformer
+from typing import Optional
+from torch import Tensor
+from torch.nn import Module, MultiheadAttention, Linear, Dropout, LayerNorm
+from torch.nn.modules.transformer import _get_activation_fn
 from torch.utils.checkpoint import checkpoint
 
 
diff -ruN -x __pycache__ tabpfn_v1/model_configs.py tabpfn_v1/model_configs.py
--- tabpfn_v1/model_configs.py	2026-09-08 13:41:29.936259358 +0900
+++ tabpfn_v1/model_configs.py	2026-09-04 15:30:53.554779397 +0900
@@ -1,9 +1,9 @@
 from copy import deepcopy
-from tabpfn.priors.utils import uniform_int_sampler_f
-from tabpfn.priors.differentiable_prior import DifferentiableHyperparameter
+from tabpfn_v1.priors.utils import uniform_int_sampler_f
+from tabpfn_v1.priors.differentiable_prior import DifferentiableHyperparameter
 from ConfigSpace import hyperparameters as CSH
 import torch
-from tabpfn.priors.differentiable_prior import replace_differentiable_distributions
+from tabpfn_v1.priors.differentiable_prior import replace_differentiable_distributions
 
 import ConfigSpace as CS
 
diff -ruN -x __pycache__ tabpfn_v1/priors/differentiable_prior.py tabpfn_v1/priors/differentiable_prior.py
--- tabpfn_v1/priors/differentiable_prior.py	2026-09-08 13:41:29.936419164 +0900
+++ tabpfn_v1/priors/differentiable_prior.py	2026-09-04 15:30:53.559402103 +0900
@@ -3,7 +3,7 @@
 import math
 
 from .utils import get_batch_to_dataloader
-from tabpfn.utils import default_device, normalize_by_used_features_f
+from tabpfn_v1.utils import default_device, normalize_by_used_features_f
 from .utils import order_by_y
 
 from .utils import trunc_norm_sampler_f, beta_sampler_f, gamma_sampler_f, uniform_sampler_f, zipf_sampler_f, scaled_beta_sampler_f, uniform_int_sampler_f
diff -ruN -x __pycache__ tabpfn_v1/priors/fast_gp.py tabpfn_v1/priors/fast_gp.py
--- tabpfn_v1/priors/fast_gp.py	2026-09-08 13:41:29.936433275 +0900
+++ tabpfn_v1/priors/fast_gp.py	2026-09-04 15:30:53.560304979 +0900
@@ -5,7 +5,7 @@
 import gpytorch
 
 from .utils import get_batch_to_dataloader
-from tabpfn.utils import default_device
+from tabpfn_v1.utils import default_device
 
 
 # We will use the simplest form of GP model, exact inference
diff -ruN -x __pycache__ tabpfn_v1/priors/flexible_categorical.py tabpfn_v1/priors/flexible_categorical.py
--- tabpfn_v1/priors/flexible_categorical.py	2026-09-08 13:41:29.936445615 +0900
+++ tabpfn_v1/priors/flexible_categorical.py	2026-09-04 15:30:53.558464069 +0900
@@ -5,7 +5,7 @@
 from torch import nn
 
 from .utils import get_batch_to_dataloader
-from tabpfn.utils import normalize_data, nan_handling_missing_for_unknown_reason_value, nan_handling_missing_for_no_reason_value, nan_handling_missing_for_a_reason_value, to_ranking_low_mem, remove_outliers, normalize_by_used_features_f
+from tabpfn_v1.utils import normalize_data, nan_handling_missing_for_unknown_reason_value, nan_handling_missing_for_no_reason_value, nan_handling_missing_for_a_reason_value, to_ranking_low_mem, remove_outliers, normalize_by_used_features_f
 from .utils import randomize_classes, CategoricalActivation
 from .utils import uniform_int_sampler_f
 
diff -ruN -x __pycache__ tabpfn_v1/priors/mlp.py tabpfn_v1/priors/mlp.py
--- tabpfn_v1/priors/mlp.py	2026-09-08 13:41:29.936459466 +0900
+++ tabpfn_v1/priors/mlp.py	2026-09-04 15:30:53.558779475 +0900
@@ -5,7 +5,7 @@
 from torch import nn
 import numpy as np
 
-from tabpfn.utils import default_device
+from tabpfn_v1.utils import default_device
 from .utils import get_batch_to_dataloader
 
 class GaussianNoise(nn.Module):
diff -ruN -x __pycache__ tabpfn_v1/priors/prior_bag.py tabpfn_v1/priors/prior_bag.py
--- tabpfn_v1/priors/prior_bag.py	2026-09-08 13:41:29.936482833 +0900
+++ tabpfn_v1/priors/prior_bag.py	2026-09-04 15:30:53.560668965 +0900
@@ -1,7 +1,7 @@
 import torch
 
 from .utils import get_batch_to_dataloader
-from tabpfn.utils import default_device
+from tabpfn_v1.utils import default_device
 
 def get_batch(batch_size, seq_len, num_features, device=default_device
               , hyperparameters=None, batch_size_per_gp_sample=None, **kwargs):
diff -ruN -x __pycache__ tabpfn_v1/priors/utils.py tabpfn_v1/priors/utils.py
--- tabpfn_v1/priors/utils.py	2026-09-08 13:41:29.936493438 +0900
+++ tabpfn_v1/priors/utils.py	2026-09-04 15:30:53.560531481 +0900
@@ -2,7 +2,7 @@
 
 import torch
 
-from tabpfn.utils import set_locals_in_self
+from tabpfn_v1.utils import set_locals_in_self
 from .prior import PriorDataLoader
 from torch import nn
 import numpy as np
diff -ruN -x __pycache__ tabpfn_v1/scripts/differentiable_pfn_evaluation.py tabpfn_v1/scripts/differentiable_pfn_evaluation.py
--- tabpfn_v1/scripts/differentiable_pfn_evaluation.py	2026-09-08 13:41:29.936546340 +0900
+++ tabpfn_v1/scripts/differentiable_pfn_evaluation.py	2026-09-04 15:30:53.563249221 +0900
@@ -3,13 +3,13 @@
 import numpy as np
 import time
 import pickle
-from tabpfn.scripts import  tabular_metrics
-from tabpfn.scripts.tabular_metrics import calculate_score_per_method
-from tabpfn.scripts.tabular_evaluation import evaluate
+from tabpfn_v1.scripts import  tabular_metrics
+from tabpfn_v1.scripts.tabular_metrics import calculate_score_per_method
+from tabpfn_v1.scripts.tabular_evaluation import evaluate
 from tqdm import tqdm
 import random
-from tabpfn.scripts.transformer_prediction_interface import get_params_from_config
-from tabpfn.scripts.transformer_prediction_interface import load_model_workflow
+from tabpfn_v1.scripts.transformer_prediction_interface import get_params_from_config
+from tabpfn_v1.scripts.transformer_prediction_interface import load_model_workflow
 
 """
 ===============================
diff -ruN -x __pycache__ tabpfn_v1/scripts/model_builder.py tabpfn_v1/scripts/model_builder.py
--- tabpfn_v1/scripts/model_builder.py	2026-09-08 13:41:29.936558897 +0900
+++ tabpfn_v1/scripts/model_builder.py	2026-09-04 15:33:26.685790563 +0900
@@ -1,8 +1,8 @@
 from functools import partial
-import tabpfn.encoders as encoders
+import tabpfn_v1.encoders as encoders
 
-from tabpfn.transformer import TransformerModel
-from tabpfn.utils import get_uniform_single_eval_pos_sampler
+from tabpfn_v1.transformer import TransformerModel
+from tabpfn_v1.utils import get_uniform_single_eval_pos_sampler
 import torch
 import math
 
@@ -43,7 +43,10 @@
     cannot be used for further training.
     """
 
-    model_state, optimizer_state, config_sample = torch.load(os.path.join(path, filename), map_location='cpu')
+    # torch>=2.6 defaults weights_only=True, which rejects the v1 checkpoint's
+    # (state_dict, None, config-dict) tuple.  The file is the official v1.0.0 ckpt.
+    model_state, optimizer_state, config_sample = torch.load(os.path.join(path, filename), map_location='cpu',
+                                                             weights_only=False)
 
     if (('nan_prob_no_reason' in config_sample and config_sample['nan_prob_no_reason'] > 0.0) or
         ('nan_prob_a_reason' in config_sample and config_sample['nan_prob_a_reason'] > 0.0) or
@@ -85,7 +88,7 @@
     # print('Loading....')
     print('!! Warning: GPyTorch must be installed !!')
     model_state, optimizer_state, config_sample = torch.load(
-        os.path.join(path, filename), map_location='cpu')
+        os.path.join(path, filename), map_location='cpu', weights_only=False)
     if ('differentiable_hyperparameters' in config_sample
             and 'prior_mlp_activations' in config_sample['differentiable_hyperparameters']):
         config_sample['differentiable_hyperparameters']['prior_mlp_activations']['choice_values_used'] = config_sample[
@@ -130,7 +133,7 @@
     copy_to_sample('differentiable_hyperparameters','prior_mlp_activations','choice_values')
 
 def load_config_sample(path, template_config):
-    model_state, optimizer_state, loaded_config_sample = torch.load(path, map_location='cpu')
+    model_state, optimizer_state, loaded_config_sample = torch.load(path, map_location='cpu', weights_only=False)
     fix_loaded_config_sample(loaded_config_sample, template_config)
     return loaded_config_sample
 
@@ -143,7 +146,7 @@
     return bptt, eval_positions, max_features, max_splits
 
 def get_mlp_prior_hyperparameters(config):
-    from tabpfn.priors.utils import gamma_sampler_f
+    from tabpfn_v1.priors.utils import gamma_sampler_f
     config = {hp: (list(config[hp].values())[0]) if type(config[hp]) is dict else config[hp] for hp in config}
 
     if 'random_feature_rotation' not in config:
@@ -173,7 +176,7 @@
 
 
 def get_meta_gp_prior_hyperparameters(config):
-    from tabpfn.priors.utils import trunc_norm_sampler_f
+    from tabpfn_v1.priors.utils import trunc_norm_sampler_f
     config = {hp: (list(config[hp].values())[0]) if type(config[hp]) is dict else config[hp] for hp in config}
 
     if "outputscale_mean" in config:
@@ -189,8 +192,8 @@
 
 
 def get_model(config, device, should_train=True, verbose=False, state_dict=None, epoch_callback=None):
-    import tabpfn.priors as priors
-    from tabpfn.train import train, Losses
+    import tabpfn_v1.priors as priors
+    from tabpfn_v1.train import train, Losses
     extra_kwargs = {}
     verbose_train, verbose_prior = verbose >= 1, verbose >= 2
     config['verbose'] = verbose_prior
diff -ruN -x __pycache__ tabpfn_v1/scripts/model_configs.py tabpfn_v1/scripts/model_configs.py
--- tabpfn_v1/scripts/model_configs.py	2026-09-08 13:41:29.936572550 +0900
+++ tabpfn_v1/scripts/model_configs.py	2026-09-04 15:30:53.561068537 +0900
@@ -1,9 +1,9 @@
 from copy import deepcopy
-from tabpfn.priors.utils import uniform_int_sampler_f
-from tabpfn.priors.differentiable_prior import DifferentiableHyperparameter
+from tabpfn_v1.priors.utils import uniform_int_sampler_f
+from tabpfn_v1.priors.differentiable_prior import DifferentiableHyperparameter
 from ConfigSpace import hyperparameters as CSH
 import torch
-from tabpfn.priors.differentiable_prior import replace_differentiable_distributions
+from tabpfn_v1.priors.differentiable_prior import replace_differentiable_distributions
 
 import ConfigSpace as CS
 
diff -ruN -x __pycache__ tabpfn_v1/scripts/tabular_baselines.py tabpfn_v1/scripts/tabular_baselines.py
--- tabpfn_v1/scripts/tabular_baselines.py	2026-09-08 13:41:29.936586759 +0900
+++ tabpfn_v1/scripts/tabular_baselines.py	2026-09-08 13:21:26.520798514 +0900
@@ -26,14 +26,15 @@
 from sklearn.gaussian_process import GaussianProcessClassifier
 from sklearn.gaussian_process.kernels import RBF
 import numpy as np
-np.warnings.filterwarnings('ignore', category=np.VisibleDeprecationWarning) 
+import warnings as _warnings  # numpy>=1.24 removed np.warnings; numpy 2 moved VisibleDeprecationWarning
+_warnings.filterwarnings('ignore', category=getattr(getattr(np, 'exceptions', np), 'VisibleDeprecationWarning', DeprecationWarning))
 
 import torch
 import itertools
-from tabpfn.scripts import tabular_metrics
+from tabpfn_v1.scripts import tabular_metrics
 import pandas as pd
 from tqdm import tqdm
-from tabpfn.utils import remove_outliers
+from tabpfn_v1.utils import remove_outliers
 
 from sklearn.linear_model import LogisticRegression, Ridge
 from sklearn.model_selection import cross_val_score
@@ -241,7 +242,7 @@
 import random
 from tqdm import tqdm
 def transformer_metric(x, y, test_x, test_y, cat_features, metric_used, max_time=300, device='cpu', N_ensemble_configurations=3, classifier=None):
-    from tabpfn.scripts.transformer_prediction_interface import TabPFNClassifier
+    from tabpfn_v1.scripts.transformer_prediction_interface import TabPFNClassifier
 
     if classifier is None:
       classifier = TabPFNClassifier(device=device, N_ensemble_configurations=N_ensemble_configurations)
diff -ruN -x __pycache__ tabpfn_v1/scripts/tabular_evaluation.py tabpfn_v1/scripts/tabular_evaluation.py
--- tabpfn_v1/scripts/tabular_evaluation.py	2026-09-08 13:41:29.936618978 +0900
+++ tabpfn_v1/scripts/tabular_evaluation.py	2026-09-04 15:30:53.562109005 +0900
@@ -11,11 +11,11 @@
 from torch import nn
 
 from torch.utils.checkpoint import checkpoint
-from tabpfn.utils import normalize_data, torch_nanmean, to_ranking_low_mem, remove_outliers
-from tabpfn.scripts.tabular_baselines import get_scoring_string
-from tabpfn.scripts import tabular_metrics
-from tabpfn.scripts.transformer_prediction_interface import *
-from tabpfn.scripts.baseline_prediction_interface import *
+from tabpfn_v1.utils import normalize_data, torch_nanmean, to_ranking_low_mem, remove_outliers
+from tabpfn_v1.scripts.tabular_baselines import get_scoring_string
+from tabpfn_v1.scripts import tabular_metrics
+from tabpfn_v1.scripts.transformer_prediction_interface import *
+from tabpfn_v1.scripts.baseline_prediction_interface import *
 """
 ===============================
 PUBLIC FUNCTIONS FOR EVALUATION
diff -ruN -x __pycache__ tabpfn_v1/scripts/transformer_prediction_interface.py tabpfn_v1/scripts/transformer_prediction_interface.py
--- tabpfn_v1/scripts/transformer_prediction_interface.py	2026-09-08 13:41:29.936645812 +0900
+++ tabpfn_v1/scripts/transformer_prediction_interface.py	2026-09-04 15:36:02.863874428 +0900
@@ -4,8 +4,8 @@
 
 from torch.utils.checkpoint import checkpoint
 
-from tabpfn.utils import normalize_data, to_ranking_low_mem, remove_outliers
-from tabpfn.utils import NOP, normalize_by_used_features_f
+from tabpfn_v1.utils import normalize_data, to_ranking_low_mem, remove_outliers
+from tabpfn_v1.utils import NOP, normalize_by_used_features_f
 
 from sklearn.preprocessing import PowerTransformer, QuantileTransformer, RobustScaler
 
@@ -17,7 +17,7 @@
 from sklearn.utils import column_or_1d
 from sklearn.preprocessing import LabelEncoder
 from pathlib import Path
-from tabpfn.scripts.model_builder import load_model, load_model_only_inference
+from tabpfn_v1.scripts.model_builder import load_model, load_model_only_inference
 import os
 import pickle
 import io
@@ -65,13 +65,12 @@
     def check_file(e):
         model_file, model_path, results_file = get_file(e)
         if not Path(model_path).is_file():  # or Path(results_file).is_file():
-            print('We have to download the TabPFN, as there is no checkpoint at ', model_path)
-            print('It has about 100MB, so this might take a moment.')
-            import requests
-            url = 'https://github.com/automl/TabPFN/raw/main/tabpfn/models_diff/prior_diff_real_checkpoint_n_0_epoch_42.cpkt'
-            r = requests.get(url, allow_redirects=True)
-            os.makedirs(os.path.dirname(model_path), exist_ok=True)
-            open(model_path, 'wb').write(r.content)
+            # Upstream download URL is dead (404 page would be saved as a bogus
+            # checkpoint) -- see the note in ../../scripts/transformer_prediction_interface.py
+            raise FileNotFoundError(
+                f'TabPFN v1 checkpoint not found at {model_path}. Copy '
+                'prior_diff_real_checkpoint_n_0_epoch_42.cpkt (TabPFN v1.0.0) to '
+                'models_diff/prior_diff_real_checkpoint_n_0_epoch_100.cpkt.')
         return model_file, model_path, results_file
 
     model_file = None
@@ -215,7 +214,7 @@
         """
         if self.no_grad:
             # Check that X and y have correct shape
-            X, y = check_X_y(X, y, force_all_finite=False)
+            X, y = check_X_y(X, y, ensure_all_finite=False)  # sklearn>=1.6 renamed force_all_finite
         # Store the classes seen during fit
         y = self._validate_targets(y)
         self.label_encoder = LabelEncoder()
@@ -247,7 +246,7 @@
 
         # Input validation
         if self.no_grad:
-            X = check_array(X, force_all_finite=False)
+            X = check_array(X, ensure_all_finite=False)  # sklearn>=1.6 renamed force_all_finite
             X_full = np.concatenate([self.X_, X], axis=0)
             X_full = torch.tensor(X_full, device=self.device).float().unsqueeze(1)
         else:
@@ -513,11 +512,22 @@
                                     message="None of the inputs have requires_grad=True. Gradients will be None")
             warnings.filterwarnings("ignore",
                                     message="torch.cuda.amp.autocast only affects CUDA ops, but CUDA is not available.  Disabling.")
-            if device == 'cpu':
-                output_batch = checkpoint(predict, batch_input, batch_label, style_, softmax_temperature_, True)
+            # torch>=2.4 requires an explicit use_reentrant for checkpoint(); in the
+            # no_grad inference path checkpointing is a no-op, so call predict directly.
+            # torch.cuda.amp.autocast is deprecated -> torch.autocast('cuda').
+            if no_grad:
+                if device == 'cpu':
+                    output_batch = predict(batch_input, batch_label, style_, softmax_temperature_, True)
+                else:
+                    with torch.autocast('cuda', enabled=fp16_inference):
+                        output_batch = predict(batch_input, batch_label, style_, softmax_temperature_, True)
+            elif device == 'cpu':
+                output_batch = checkpoint(predict, batch_input, batch_label, style_, softmax_temperature_, True,
+                                          use_reentrant=False)
             else:
-                with torch.cuda.amp.autocast(enabled=fp16_inference):
-                    output_batch = checkpoint(predict, batch_input, batch_label, style_, softmax_temperature_, True)
+                with torch.autocast('cuda', enabled=fp16_inference):
+                    output_batch = checkpoint(predict, batch_input, batch_label, style_, softmax_temperature_, True,
+                                              use_reentrant=False)
         outputs += [output_batch]
     #print('MODEL INFERENCE TIME ('+str(batch_input.device)+' vs '+device+', '+str(fp16_inference)+')', str(time.time()-start))
 
diff -ruN -x __pycache__ tabpfn_v1/train.py tabpfn_v1/train.py
--- tabpfn_v1/train.py	2026-09-08 13:41:29.936296807 +0900
+++ tabpfn_v1/train.py	2026-09-04 15:30:53.557779456 +0900
@@ -10,13 +10,13 @@
 import torch
 from torch import nn
 
-import tabpfn.utils as utils
-from tabpfn.transformer import TransformerModel
-from tabpfn.utils import get_cosine_schedule_with_warmup, get_openai_lr, StoreDictKeyPair, get_weighted_single_eval_pos_sampler, get_uniform_single_eval_pos_sampler
-import tabpfn.priors as priors
-import tabpfn.encoders as encoders
-import tabpfn.positional_encodings as positional_encodings
-from tabpfn.utils import init_dist
+import tabpfn_v1.utils as utils
+from tabpfn_v1.transformer import TransformerModel
+from tabpfn_v1.utils import get_cosine_schedule_with_warmup, get_openai_lr, StoreDictKeyPair, get_weighted_single_eval_pos_sampler, get_uniform_single_eval_pos_sampler
+import tabpfn_v1.priors as priors
+import tabpfn_v1.encoders as encoders
+import tabpfn_v1.positional_encodings as positional_encodings
+from tabpfn_v1.utils import init_dist
 from torch.cuda.amp import autocast, GradScaler
 from torch import nn
 
diff -ruN -x __pycache__ tabpfn_v1/transformer.py tabpfn_v1/transformer.py
--- tabpfn_v1/transformer.py	2026-09-08 13:41:29.936311361 +0900
+++ tabpfn_v1/transformer.py	2026-09-04 15:30:53.555518238 +0900
@@ -6,8 +6,8 @@
 from torch import Tensor
 from torch.nn import Module, TransformerEncoder
 
-from tabpfn.layer import TransformerEncoderLayer, _get_activation_fn
-from tabpfn.utils import SeqBN, bool_mask_to_att_mask
+from tabpfn_v1.layer import TransformerEncoderLayer, _get_activation_fn
+from tabpfn_v1.utils import SeqBN, bool_mask_to_att_mask
 
 
 
