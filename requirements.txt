# Requirements for cic_1.py
# Network Intrusion Detection - XGBoost Mixture-of-Experts

# Core dependencies
numpy>=1.25.2
pandas>=2.0.0
scipy>=1.11.4

# Machine Learning
scikit-learn>=1.4.2
xgboost>=2.0.0
imbalanced-learn>=0.11.0

# Deep Learning (for CUDA device detection + CIFAR-LT dataset)
# WARNING: the torch WHEEL's CUDA build must match the node's driver, and pip
# will happily install a wheel that is too new. Symptom of a mismatch:
#   UserWarning: CUDA initialization: The NVIDIA driver ... is too old
#   -> torch.cuda.is_available() False -> --device auto silently falls back to
#      CPU -> TabPFN aborts with "Running on CPU with more than 5000 samples".
# Pinned to the cu124 build below: CUDA wheels are driver-backward-compatible,
# so this installs clean on every node seen so far (driver 550.x/CUDA 12.4 A100
# pods, driver 580.x/CUDA 13.0 local RTX 4090) without a manual reinstall step.
# tabpfn 8.2.0 requires torch>=2.5, so cu124 builds below 2.5 will not do.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision>=0.15.0

# GPU acceleration (optional, for CUDA support)
cupy-cuda12x>=12.0.0  # CUDA 12.x용, 다른 CUDA 버전은 cupy-cuda11x 등으로 변경

# Data processing
joblib>=1.2.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bar
tqdm>=4.65.0

tabpfn

